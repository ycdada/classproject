# 两套基线都要过 30%，定量加观感

完善版必须同时对照当前可运行源码和 `原文档/` 达到可度量的 30% 差异。源码用文件哈希存活率、核心模块替换比例、文档重写比例三联验收，并保留度量脚本；学术四件套（计划书、中期报告、开发文档、答辩 PPT）每件都要章节大纲变更且段落替换 ≥30%。除此之外，完成后必须看起来像新版（新架构图、新栈、题目草稿主线），不能只是定量过线的换皮。

差异必须来自模块替换与文档重写，不能靠改名、重排或灌水行数。
